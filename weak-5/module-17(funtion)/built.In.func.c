#include <stdio.h>
#include <math.h>

int main() {
    double a = 10.47;
    printf("%f\n", ceil(a)); // Use %f for printing double

    // Uncomment these lines to use them:
    // printf("%f\n", floor(a));
    // printf("%f\n", sqrt(a));
    // printf("%f\n", pow(2, 3));
    // printf("%f\n", log(2));

    return 0;
}
